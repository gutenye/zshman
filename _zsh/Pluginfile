#theme local
#theme default.rvm.ssh.git.svn # rvm, svn are slow.
theme local.user.rbenv.ssh.git

keymap vim 
#keymap local

plugin utils 
plugin autojump

plugin archlinux
  #plugin yaourt
#plugin debian
#plugin fedora
plugin systemd
plugin iproute2
#plugin ssh-agent

plugin ruby
  plugin rails
  plugin rbenv
  #plugin rvm
plugin python
plugin node
#plugin perl

plugin vim
  #plugin vundle
plugin git
#plugin github

# vim: filetype=zsh
