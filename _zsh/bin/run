#!/bin/zsh

# run a gui app in terminal.
#
# Usage:
#   run APP ARGS ..
#
# Configure file: ~/.runrc
#

app=$1
shift

source ~/.runrc

if (( ${+apps[$app]} )); then
  cmd=$apps[$app]
  $cmd &>/dev/null &
else
  $app $* &>/dev/null &
fi
